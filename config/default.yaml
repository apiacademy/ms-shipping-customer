app:
  port: 3000
# Only set base_url if you have to due to some proxy problems. Code should auto-discover it.
# base_url: "docker:5001"
  media_type: "application/vnd.siren+json"
  pub_dir: "/public"
  csrf: false
  cookie_secret: "Your secret key salt goes here. Replace with random, long string in your project."
